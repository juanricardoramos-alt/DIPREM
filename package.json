{
  "name": "diprem-crm",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "db:migrate": "node scripts/db/migrate.mjs",
    "db:aplicar": "node scripts/db/aplicar-sql.mjs",
    "db:validar": "bash scripts/db/validar-local.sh",
    "db:reset": "supabase db reset",
    "db:start": "supabase start"
  },
  "devDependencies": {
    "pg": "^8.22.0",
    "turbo": "^2.5.0"
  },
  "pnpm": {
    "overrides": {
      "@types/react": ">=19.1.0 <19.2.0",
      "@types/react-dom": ">=19.1.0 <19.2.0"
    }
  }
}
