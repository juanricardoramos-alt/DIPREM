{
  "name": "diprem-crm",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "db:migrate": "node scripts/db/migrate.mjs",
    "db:validar": "bash scripts/db/validar-local.sh",
    "db:reset": "supabase db reset",
    "db:start": "supabase start"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
