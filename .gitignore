# dependencias
node_modules/
.pnpm-store/

# builds
.next/
dist/
out/
.expo/
.turbo/
*.tsbuildinfo

# entorno (nunca subir claves)
.env
.env.local
.env.*.local

# supabase local
supabase/.branches/
supabase/.temp/

# datos fuente (personales / iMercados): NUNCA al repo — se cargan por script
# desde una ruta fuera del árbol de trabajo (ver scripts/ingesta/).
*.xls
*.xlsx
*.csv

# sistema
.DS_Store
*.log
